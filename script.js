document.getElementById("search-button").addEventListener("click", function() {
    const query = document.getElementById("search-input").value;
    const resultsContainer = document.getElementById("search-results");
    
    resultsContainer.innerHTML = ""; // Limpa resultados anteriores

    if (query) {
        // Simulação de resultados de pesquisa
        const mockResults = [
            `Resultado 1 para "${Suzane von Richthofen}"`,
            `Resultado 2 para "${A Chacina da Família de Ceilândia}"`,
            `Resultado 3 para "${Caso Eloá Cristina}"`
        ];

        mockResults.forEach(result => {
            const resultItem = document.createElement("div");
            resultItem.textContent = result;
            resultsContainer.appendChild(resultItem);
        });
    } else {
        alert("Por favor, insira um termo de pesquisa.");
    }
});

document.addEventListener('DOMContentLoaded', function() {
    const nome = document.getElementById('nome');
    const email = document.getElementById('email');
    const mensagem = document.getElementById('mensagem');
    const form = document.querySelector('form');

    con

   

    const form = document.getElementById("conta

    const form = document.getElementById("co

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("nam

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("ema

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("mess

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameE

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailEr

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("ema

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageErr

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("mess

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMe

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("succes

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("su

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatóri

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obriga

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nom

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O 

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("erro

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("e

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mai

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("err

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail vál

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail 

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-ma

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatór

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obri

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem 

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter entre 10 e 500 caracte

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter entre 10 e 500 ca

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter entre 

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter e

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensage

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mens

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A 

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter entre 10 e 500 caracteres.";
        messageField.classList.add("

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter entre 10 e 500 caracteres.";
        messageField.classList.add("error");
        return false;
      }
      messageError.textContent = "

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter entre 10 e 500 caracteres.";
        messageField.classList.add("error");
        return false;
      }
      messageError.textContent = "";
      messageField.classList.remove("e

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter entre 10 e 500 caracteres.";
        messageField.classList.add("error");
        return false;
      }
      messageError.textContent = "";
      messageField.classList.remove("error");
      return true;
    }

    // Eventos de validação em tempo real
    nameField.addEventListener("inpu

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter entre 10 e 500 caracteres.";
        messageField.classList.add("error");
        return false;
      }
      messageError.textContent = "";
      messageField.classList.remove("error");
      return true;
    }

    // Eventos de validação em tempo real
    nameField.addEventListener("input", validateName);
    emailField.addEventListener("i

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter entre 10 e 500 caracteres.";
        messageField.classList.add("error");
        return false;
      }
      messageError.textContent = "";
      messageField.classList.remove("error");
      return true;
    }

    // Eventos de validação em tempo real
    nameField.addEventListener("input", validateName);
    emailField.addEventListener("input", validateEmail);
    messageField.addEventListener("input", validateMessage);

    // Validação ao enviar o formulário
    form.addEventListener("submit", function (event) {
      event.preventDefault();

      const isNameValid = validateName();
      const isEmailValid = validateEmail();
      const isMessageValid = validateMessage();

      if (isNameValid && isEmailValid && isMessageValid) {
        successMessage.style.display = "bloc

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter entre 10 e 500 caracteres.";
        messageField.classList.add("error");
        return false;
      }
      messageError.textContent = "";
      messageField.classList.remove("error");
      return true;
    }

    // Eventos de validação em tempo real
    nameField.addEventListener("input", validateName);
    emailField.addEventListener("input", validateEmail);
    messageField.addEventListener("input", validateMessage);

    // Validação ao enviar o formulário
    form.addEventListener("submit", function (event) {
      event.preventDefault();

      const isNameValid = validateName();
      const isEmailValid = validateEmail();
      const isMessageValid = validateMessage();

      if (isNameValid && isEmailValid && isMessageValid) {
        successMessage.style.display = "

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter entre 10 e 500 caracteres.";
        messageField.classList.add("error");
        return false;
      }
      messageError.textContent = "";
      messageField.classList.remove("error");
      return true;
    }

    // Eventos de validação em tempo real
    nameField.addEventListener("input", validateName);
    emailField.addEventListener("input", validateEmail);
    messageField.addEventListener("input", validateMessage);

    // Validação ao enviar o formulário
    form.addEventListener("submit", function (event) {
      event.preventDefault();

      const isNameValid = validateName();
      const isEmailValid = validateEmail();
      const isMessageValid = validateMessage();

      if (isNameValid && isEmailValid && isMessageValid) {
        successMessage.style.display = "block";
        form.reset();
      } else {
        successMessage.style.display = "n

    const form = document.getElementById("contactForm");
    const nameField = document.getElementById("name");
    const emailField = document.getElementById("email");
    const messageField = document.getElementById("message");

    const nameError = document.getElementById("nameError");
    const emailError = document.getElementById("emailError");
    const messageError = document.getElementById("messageError");
    const successMessage = document.getElementById("successMessage");

    // Funções de validação
    function validateName() {
      const value = nameField.value.trim();
      if (!value) {
        nameError.textContent = "O nome é obrigatório.";
        nameField.classList.add("error");
        return false;
      } else if (value.split(/\s+/).length > 3) {
        nameError.textContent = "O nome deve ter no máximo 3 palavras.";
        nameField.classList.add("error");
        return false;
      }
      nameError.textContent = "";
      nameField.classList.remove("error");
      return true;
    }

    function validateEmail() {
      const value = emailField.value.trim();
      if (!value) {
        emailError.textContent = "O e-mail é obrigatório.";
        emailField.classList.add("error");
        return false;
      } else if (!/\S+@\S+\.\S+/.test(value)) {
        emailError.textContent = "Digite um e-mail válido.";
        emailField.classList.add("error");
        return false;
      }
      emailError.textContent = "";
      emailField.classList.remove("error");
      return true;
    }

    function validateMessage() {
      const value = messageField.value.trim();
      if (!value) {
        messageError.textContent = "A mensagem é obrigatória.";
        messageField.classList.add("error");
        return false;
      } else if (value.length < 10 || value.length > 500) {
        messageError.textContent = "A mensagem deve ter entre 10 e 500 caracteres.";
        messageField.classList.add("error");
        return false;
      }
      messageError.textContent = "";
      messageField.classList.remove("error");
      return true;
    }

    // Eventos de validação em tempo real
    nameField.addEventListener("input", validateName);
    emailField.addEventListener("input", validateEmail);
    messageField.addEventListener("input", validateMessage);

    // Validação ao enviar o formulário
    form.addEventListener("submit", function (event) {
      event.preventDefault();

      const isNameValid = validateName();
      const isEmailValid = validateEmail();
      const isMessageValid = validateMessage();

      if (isNameValid && isEmailValid && isMessageValid) {
        successMessage.style.display = "block";
        form.reset();
      } else {
        successMessage.style.display = "none";
      }
    });
  </script>

</body>

</html